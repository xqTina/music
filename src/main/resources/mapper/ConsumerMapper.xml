<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.yin.mapper.ConsumerMapper">
    <resultMap id="BaseResultMap" type="com.example.yin.model.domin.Consumer">
        <id property="id" column="id" jdbcType="INTEGER"></id>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="sex" property="sex" jdbcType="TINYINT" />
        <result column="phone_num" property="phoneNum" jdbcType="CHAR" />
        <result column="birth" property="birth" jdbcType="TIMESTAMP" />
        <result column="introduction" property="introduction" jdbcType="VARCHAR" />
        <result column="location" property="location" jdbcType="VARCHAR" />
        <result column="avator" property="avator" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <sql id="Base_Column_List">
        id, username, password, sex, phone_num, birth, introduction, location, avator, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.example.yin.model.domin.Consumer">
        insert into consumer
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id !=null">
                id,
            </if>
            <if test="username != null">
                usernamae,
            </if>
            <if test="password !=null">
                password,
            </if>
            <if test="sex !=null">
                sex,
            </if>
            <if test="phoneNum !=null">
                phone_num,
            </if>
            <if test="birth !=null">
                birth,
            </if>
            <if test="introduction !=null">
                introduction,
            </if>
            <if test="location !=null">
                location,
            </if>
            <if test="avator !=null">
                avator,
            </if>
             create_time,update_time,
        </trim>
        <trim prefix=" values (" suffix=")" suffixOverrides=",">
            <if test="id !=null">
                #{id},
            </if>
            <if test="username != null">
                #{usernamae},
            </if>
            <if test="password !=null">
                #{password},
            </if>
            <if test="sex !=null">
                #{sex},
            </if>
            <if test="phoneNum !=null">
                #{phoneNum},
            </if>
            <if test="birth !=null">
                #{birth},
            </if>
            <if test="introduction !=null">
                #{introduction},
            </if>
            <if test="location !=null">
                #{location},
            </if>
            <if test="avator !=null">
                #{avator},
            </if>
            now().now(),
        </trim>

    </insert>

    <update id="update" parameterType="com.example.yin.model.domin.Consumer">
        update consumer
        <set>
            <if test="username != null">
                username = #{usernamae},
            </if>
            <if test="password !=null">
                password = #{password},
            </if>
            <if test="sex !=null">
                sex = #{sex},
            </if>
            <if test="phoneNum !=null">
                phone_num =  #{phoneNum},
            </if>
            <if test="birth !=null">
                birth = #{birth},
            </if>
            <if test="introduction !=null">
                introduction = #{introduction},
            </if>
            <if test="location !=null">
                location = #{location},
            </if>
            <if test="avator !=null">
                avator =  #{avator},
            </if>
            update_time = now(),
        </set>


    </update>
    <delete id="delete" parameterType="java.lang.Integer">
        delete from consumer
        from id=#{id}
    </delete>

    <select id="verifyPassword" resultType="java.lang.Integer">
        select count(*) from consumer where name=#{0} and password=#{1}
    </select>
    <select id="editPassword" resultType="java.lang.Integer" parameterType="java.util.HashMap">
        select count(*)
        from consumer
        where username = #{username} and password = #{password}
    </select>
    <select id="getByUsername" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_column_List"/>
        from consumer
        where username = #{username}
    </select>





</mapper>